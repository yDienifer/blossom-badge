import { renderForm, formAction } from './form.js';

// 🌸 função para iniciar o aplicativo !!
const startApp = () => {
  renderForm(); // 🌸 exibe o formulário !!
  formAction(); // 🌸 aplica lógica para o formulário !!
}

startApp();
